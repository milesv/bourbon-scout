# Bourbon Scout

Allocated bourbon inventory scraper with Discord webhook alerts. Monitors 6 retailers for 25 rare/allocated bottles near a zip code, sends per-store Discord reports as each store is scanned.

## Architecture

- **`scraper.js`** — Main entry point. Runs a cron-scheduled poll loop that checks each discovered store per retailer and sends a Discord embed immediately per store (not batched).
- **`lib/geo.js`** — Zip-to-coordinates (`zipToCoords`) via zippopotam.us API and `haversine` distance calculation. No dependencies.
- **`lib/discover-stores.js`** — Auto-discovers nearby stores for all 6 retailers given a zip code and radius. Caches results to `stores.json` with a 7-day TTL. Uses Playwright for browser-based store locators and Kroger REST API for Kroger.
- **`state.json`** — Per-store stock state, nested as `{ "retailer": { "storeId": ["bottle", ...] } }`. Auto-generated, safe to delete for a fresh start.
- **`stores.json`** — Cached store discovery results. Auto-generated, safe to delete to force re-discovery.
- **`debug/`** — Screenshots, HTML dumps, and CSS selector lists captured from retailer store locator pages. Used for fixing selectors when sites change. Generated by `debug-locators.js`.

## Key Patterns

- **ESM throughout** — `"type": "module"` in package.json, all files use `import`/`export`
- **Playwright + Stealth** — `playwright-extra` with `puppeteer-extra-plugin-stealth` for bot detection bypass
- **Browser scrapers run sequentially** — single shared browser instance to avoid memory spikes
- **API scrapers run in parallel** — Kroger and Safeway use REST APIs, run via `Promise.all` alongside browser work
- **Store discovery is separate from scraping** — `discover-stores.js` handles finding stores, `scraper.js` handles checking inventory
- **Discord embeds sent per-store** — each store result posts to Discord immediately after scanning, not batched at end. One embed per webhook call to stay under Discord's 6000 char payload limit.

## Environment Variables

All config is in `.env`:
- `ZIP_CODE` — center point for store discovery (currently 85283 / Tempe, AZ)
- `SEARCH_RADIUS_MILES` — how far to search (default 15)
- `MAX_STORES_PER_RETAILER` — caps per chain (default 5)
- `POLL_INTERVAL` — cron expression (default `*/15 * * * *`)
- `KROGER_CLIENT_ID`, `KROGER_CLIENT_SECRET` — Kroger API OAuth credentials
- `SAFEWAY_API_KEY` — Safeway product search API key
- `DISCORD_WEBHOOK_URL` — Discord webhook for alerts

## Retailers

| Retailer | Scraper Type | Store Locator Method | Verified Selectors |
|----------|-------------|---------------------|-------------------|
| Costco | Playwright | `costco.com/warehouse-locations` | MUI `data-testid="Text_warehousetile-*"` attributes |
| Total Wine | Playwright | `totalwine.com/store-finder` | JSON-LD `@type: "LiquorStore"`, fallback `[class*="storeListItem"]` |
| Walmart | Playwright | `walmart.com/store/finder` | `h3.f4.lh-copy.b.ma0`, `p.ma0.lh-copy`, `a[href^="/store/"]` |
| Kroger | REST API | `api.kroger.com/v1/locations` | Structured JSON response |
| Safeway | REST API | `local.safeway.com/search.html` | Yext-powered JS-rendered results |
| BevMo | Playwright | `bevmo.com/pages/store-locator` | `a[href*="/pages/stores/"]` (no AZ locations) |

## Bottles (25)

Blanton's (Gold, SFTB, Special Reserve, Red), Weller (Special Reserve, Antique 107, 12 Year, Full Proof, Single Barrel, C.Y.P.B.), E.H. Taylor Small Batch, Stagg Jr, BTAC (George T. Stagg, Eagle Rare 17, William Larue Weller, Thomas H. Handy, Sazerac Rye 18), Pappy Van Winkle (10/12/15/20/23), Elmer T. Lee, Rock Hill Farms, King of Kentucky.

## Running

```sh
npm start        # Production: discover stores + start polling
npm run dev      # Development: same with --watch for auto-restart
```

## Debugging Store Locators

When a retailer's store locator stops finding stores (selectors broke):
1. Run `node debug-locators.js` to capture fresh screenshots + HTML + selector lists to `debug/`
2. Compare `debug/{retailer}-selectors.txt` against the selectors in `lib/discover-stores.js`
3. Update the locator function, delete `stores.json`, and re-run

## Gotchas

- Store locator CSS selectors in `discover-stores.js` break when retailer sites update their DOM. Kroger's API locator is the most stable.
- Safeway's store locator is at `local.safeway.com/search.html` — NOT `safeway.com/stores/search` (that 404s).
- Deleting `state.json` is safe but will send a full report for all stores on next poll.
- The Safeway scraper and Kroger scraper silently skip if their API keys aren't set.
- `stores.json` cache invalidates on zip/radius change or after 7 days.
- BevMo has no Arizona locations — the locator correctly returns 0 stores for AZ zip codes.
